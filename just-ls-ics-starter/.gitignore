# Child project-specific ignores only.
# Common Python / env / build / editor rules are handled by the repo root .gitignore.

# UI local backups / archives
ui/_archive/

# Optional local scratch outputs specific to this package
scripts/sweep_out/